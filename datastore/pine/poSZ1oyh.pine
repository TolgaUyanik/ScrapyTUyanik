//@version=6
// @description Master Library for Institutional Analysis v1
// @author jmcanovelles
library("TradingSystems_AlphaLib_v1_Final")

// @function Calculates standardized EMA
export calc_ema(int len) =>
    ta.ema(close, len)

// @function Calculates precise ADX and DI
export calc_adx(int len) =>
    [di_plus, di_minus, adx_val] = ta.dmi(len, len)
    [di_plus, di_minus, adx_val]