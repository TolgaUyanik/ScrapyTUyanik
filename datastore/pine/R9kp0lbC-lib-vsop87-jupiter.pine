//   ════════════════════════════════════════════════════════════
//      BLUEPRINT RESEARCH • OPEN SOURCE PLANETARY EPHEMERIS
//   ════════════════════════════════════════════════════════════
//
//                  ★       ★       ★
//                      ╱   │   ╲
//                     ─    ☉    ─
//                      ╲   │   ╱
//                  ★       ★       ★
//
//   ════════════════════════════════════════════════════════════
//
// @library      VSOP87 Jupiter Library
// @description  Heliocentric and geocentric position calculations for Jupiter
//               using VSOP87 theory. Provides longitude, latitude, radius, speed,
//               and declination functions.
//
// @author       BlueprintResearch (Javonnii)
// @license      MIT License - Free to use with attribution

//
// @theory       VSOP87A (Heliocentric rectangular coordinates)
// @accuracy     Truncated series (~10-15 terms per series) - arcsecond precision
// @time_scale   Julian millennia from J2000.0 (use core.get_julian_millennia)
//
// @reference    Meeus, Jean. "Astronomical Algorithms" (2nd Ed., 1998)
//               Bretagnon & Francou. "VSOP87 Solutions" (1988)
//
// @showcase     Includes commented showcase code with 250-bar future projection.
//               Uncomment to display Jupiter data with polyline projections.
//
// @open_source  This library is part of an open-source alternative to
//               proprietary astronomical libraries. Study, modify, and
//               share freely. We believe knowledge of the cosmos belongs
//               to everyone.
//
// ════════════════════════════════════════════════════════════════
//   © 2025 BlueprintResearch / Javonnii
//   Licensed under MIT License
// ════════════════════════════════════════════════════════════════
//
//@version=6
import BlueprintResearch/lib_vsop_core/1 as core
library("lib_vsop87_jupiter")


// Constants
var float PI = 3.141592653589793
var float RAD_TO_DEG = 180.0 / PI


// @function Computes Jupiter's heliocentric ecliptic longitude using VSOP87 theory.
// @param t (float) Julian millennia from J2000.0 (use core.get_julian_millennia(time)).
// @returns (float) Heliocentric ecliptic longitude in degrees, normalized to range [0, 360).
export get_helio_lon(float t) =>
    array<core.vsop_term> L0 = array.new<core.vsop_term>()
    array.push(L0, core.vsop_term.new(0.59954691494, 0.0, 0.0))
    array.push(L0, core.vsop_term.new(0.09695898711, 5.06191793105, 529.69096509460))
    array.push(L0, core.vsop_term.new(0.00573610142, 1.44406205629, 7.11354700080))
    array.push(L0, core.vsop_term.new(0.00306389180, 5.41734729976, 1059.38193018920))
    array.push(L0, core.vsop_term.new(0.00097178296, 4.14264726552, 632.78373931320))
    array.push(L0, core.vsop_term.new(0.00072903096, 3.64042909255, 522.57741809380))
    array.push(L0, core.vsop_term.new(0.00064263975, 3.41145165351, 103.09277421860))
    array.push(L0, core.vsop_term.new(0.00039806064, 2.29376740788, 419.48464387520))
    array.push(L0, core.vsop_term.new(0.00038857767, 1.27231755835, 316.39186965660))
    array.push(L0, core.vsop_term.new(0.00027964622, 1.78454591820, 536.80451209540))

    array<core.vsop_term> L1 = array.new<core.vsop_term>()
    array.push(L1, core.vsop_term.new(529.93480757497, 0.0, 0.0))
    array.push(L1, core.vsop_term.new(0.00489741194, 4.22066689928, 529.69096509460))
    array.push(L1, core.vsop_term.new(0.00228918538, 6.02647464016, 7.11354700080))
    array.push(L1, core.vsop_term.new(0.00027655380, 4.57265956824, 1059.38193018920))

    array<core.vsop_term> L2 = array.new<core.vsop_term>()
    array.push(L2, core.vsop_term.new(0.00047233601, 4.32148536482, 7.11354700080))
    array.push(L2, core.vsop_term.new(0.00014837133, 3.14159265359, 0.0))

    L0sum = core.eval_vsop87(L0, t)
    L1sum = core.eval_vsop87(L1, t)
    L2sum = core.eval_vsop87(L2, t)
    lon_rad = L0sum + L1sum * t + L2sum * t * t
    core.mod360(lon_rad * RAD_TO_DEG)


// @function Computes Jupiter's heliocentric ecliptic latitude using VSOP87 theory.
// @param t (float) Julian millennia from J2000.0 (use core.get_julian_millennia(time)).
// @returns (float) Heliocentric ecliptic latitude in radians, range approximately [-0.035, +0.035]. Note: Returns radians, not degrees.
export get_helio_lat(float t) =>
    array<core.vsop_term> B0 = array.new<core.vsop_term>()
    array.push(B0, core.vsop_term.new(0.02268615702, 3.55852606721, 529.69096509460))
    array.push(B0, core.vsop_term.new(0.00109971634, 3.90809347197, 1059.38193018920))
    array.push(B0, core.vsop_term.new(0.00110090358, 0.0, 0.0))
    core.eval_vsop87(B0, t)  // returns radians directly


// @function Computes Jupiter's heliocentric radius (distance from Sun) using VSOP87 theory.
// @param t (float) Julian millennia from J2000.0 (use core.get_julian_millennia(time)).
// @returns (float) Heliocentric radius in astronomical units (AU). Typical range is 4.95-5.46 AU.
export get_helio_radius(float t) =>
    array<core.vsop_term> R0 = array.new<core.vsop_term>()
    array.push(R0, core.vsop_term.new(5.20887429471, 0.0, 0.0))
    array.push(R0, core.vsop_term.new(0.25209327020, 3.49108640015, 529.69096509460))
    array.push(R0, core.vsop_term.new(0.00610599976, 3.84115365602, 1059.38193018920))
    array.push(R0, core.vsop_term.new(0.00282029465, 2.57419879933, 632.78373931320))
    array.push(R0, core.vsop_term.new(0.00187647346, 2.07590380082, 522.57741809380))

    array<core.vsop_term> R1 = array.new<core.vsop_term>()
    array.push(R1, core.vsop_term.new(0.01271801596, 2.64937511122, 529.69096509460))
    array.push(R1, core.vsop_term.new(0.00061661771, 3.00076251018, 1059.38193018920))

    array<core.vsop_term> R2 = array.new<core.vsop_term>()
    array.push(R2, core.vsop_term.new(0.00079644833, 1.35865896596, 529.69096509460))

    R0sum = core.eval_vsop87(R0, t)
    R1sum = core.eval_vsop87(R1, t)
    R2sum = core.eval_vsop87(R2, t)
    R0sum + R1sum * t + R2sum * t * t


// @function Computes Jupiter's geocentric longitude speed (rate of change over time).
// @param t (float) Julian millennia from J2000.0 (use core.get_julian_millennia(time)).
// @returns (float) Geocentric longitude speed in degrees per day. Negative values indicate retrograde motion (apparent backward movement).
export get_geo_speed(float t) =>
    // Jupiter terms and sums
    array<core.vsop_term> L0_j = array.new<core.vsop_term>()
    array.push(L0_j, core.vsop_term.new(0.59954691494, 0.0, 0.0))
    array.push(L0_j, core.vsop_term.new(0.09695898719, 5.06191793158, 529.69096509460))
    array.push(L0_j, core.vsop_term.new(0.00573610145, 1.44406205976, 7.11354700080))
    array.push(L0_j, core.vsop_term.new(0.00306389180, 5.41734729976, 1059.38193018920))
    array.push(L0_j, core.vsop_term.new(0.00097178296, 4.14264726552, 632.78373931320))
    array.push(L0_j, core.vsop_term.new(0.00072903096, 3.64042916389, 522.57741809380))

    array<core.vsop_term> L1_j = array.new<core.vsop_term>()
    array.push(L1_j, core.vsop_term.new(529.69096508814, 0.0, 0.0))
    array.push(L1_j, core.vsop_term.new(0.00489503243, 4.22082939470, 529.69096509460))

    array<core.vsop_term> L2_j = array.new<core.vsop_term>()
    array.push(L2_j, core.vsop_term.new(0.00047233601, 4.32148536482, 7.11354700080))

    float L0sum_j = core.eval_vsop87(L0_j, t)
    float L1sum_j = core.eval_vsop87(L1_j, t)
    float L2sum_j = core.eval_vsop87(L2_j, t)
    float lon_rad_j = L0sum_j + L1sum_j * t + L2sum_j * t * t

    float dL0_j = core.eval_vsop87_derivative(L0_j, t)
    float dL1_j = core.eval_vsop87_derivative(L1_j, t)
    float dL2_j = core.eval_vsop87_derivative(L2_j, t)
    float dlon_dt_j = dL0_j + L1sum_j + t * dL1_j + 2.0 * t * L2sum_j + t * t * dL2_j

    array<core.vsop_term> B0_j = array.new<core.vsop_term>()
    array.push(B0_j, core.vsop_term.new(0.02268615702, 3.55852606718, 529.69096509460))
    float lat_rad_j = core.eval_vsop87(B0_j, t)
    float dlat_dt_j = core.eval_vsop87_derivative(B0_j, t)

    array<core.vsop_term> R0_j = array.new<core.vsop_term>()
    array.push(R0_j, core.vsop_term.new(5.20887429471, 0.0, 0.0))
    array.push(R0_j, core.vsop_term.new(0.25209327119, 3.49108640015, 529.69096509460))

    array<core.vsop_term> R1_j = array.new<core.vsop_term>()
    array.push(R1_j, core.vsop_term.new(0.01271801520, 2.64937512894, 529.69096509460))

    float R0sum_j = core.eval_vsop87(R0_j, t)
    float R1sum_j = core.eval_vsop87(R1_j, t)
    float r_j = R0sum_j + R1sum_j * t

    float dR0_j = core.eval_vsop87_derivative(R0_j, t)
    float dR1_j = core.eval_vsop87_derivative(R1_j, t)
    float dr_dt_j = dR0_j + R1sum_j + t * dR1_j

    float cl_j = math.cos(lon_rad_j)
    float sl_j = math.sin(lon_rad_j)
    float cb_j = math.cos(lat_rad_j)
    float sb_j = math.sin(lat_rad_j)

    float x_j = r_j * cb_j * cl_j
    float y_j = r_j * cb_j * sl_j

    float vx_j = dr_dt_j * cb_j * cl_j - r_j * dlat_dt_j * sb_j * cl_j - r_j * cb_j * sl_j * dlon_dt_j
    float vy_j = dr_dt_j * cb_j * sl_j - r_j * dlat_dt_j * sb_j * sl_j + r_j * cb_j * cl_j * dlon_dt_j

    // Earth remains unchanged
    // Earth terms and sums (from get_earth_helio_coords)
    array<core.vsop_term> L0_e = array.new<core.vsop_term>()
    array.push(L0_e, core.vsop_term.new(1.75347045673, 0.0, 0.0))
    array.push(L0_e, core.vsop_term.new(0.03341656456, 4.66925680417, 6283.07584999140))
    array.push(L0_e, core.vsop_term.new(0.00034894275, 4.62610241759, 12566.15169998280))
    array.push(L0_e, core.vsop_term.new(0.00003417571, 2.82886579606, 3.52311834900))
    array.push(L0_e, core.vsop_term.new(0.00003497056, 2.74411800971, 5753.38488489680))
    array.push(L0_e, core.vsop_term.new(0.00003135896, 3.62767041756, 77713.77146812050))
    array.push(L0_e, core.vsop_term.new(0.00002676218, 4.41808345438, 7860.41939243920))
    array.push(L0_e, core.vsop_term.new(0.00002342687, 6.13516214446, 3930.20969621960))
    array.push(L0_e, core.vsop_term.new(0.00001273166, 2.03709657878, 529.69096509460))
    array.push(L0_e, core.vsop_term.new(0.00001324294, 0.74246341673, 11506.76976979360))

    array<core.vsop_term> L1_e = array.new<core.vsop_term>()
    array.push(L1_e, core.vsop_term.new(6283.07584999140, 0.0, 0.0))
    array.push(L1_e, core.vsop_term.new(0.00206058863, 2.67823455584, 6283.07584999140))
    array.push(L1_e, core.vsop_term.new(0.00004303419, 2.63512233481, 12566.15169998280))
    array.push(L1_e, core.vsop_term.new(0.00000425264, 1.59046980729, 3.52311834900))

    array<core.vsop_term> L2_e = array.new<core.vsop_term>()
    array.push(L2_e, core.vsop_term.new(0.00008721859, 1.07253635559, 6283.07584999140))
    array.push(L2_e, core.vsop_term.new(0.00000990990, 3.14159265359, 0.0))

    float L0sum_e = core.eval_vsop87(L0_e, t)
    float L1sum_e = core.eval_vsop87(L1_e, t)
    float L2sum_e = core.eval_vsop87(L2_e, t)
    float lon_rad_e = L0sum_e + L1sum_e * t + L2sum_e * t * t

    float dL0_e = core.eval_vsop87_derivative(L0_e, t)
    float dL1_e = core.eval_vsop87_derivative(L1_e, t)
    float dL2_e = core.eval_vsop87_derivative(L2_e, t)
    float dlon_dt_e = dL0_e + L1sum_e + t * dL1_e + 2.0 * t * L2sum_e + t * t * dL2_e

    array<core.vsop_term> B0_e = array.new<core.vsop_term>()
    array.push(B0_e, core.vsop_term.new(0.00000279620, 3.19870156017, 84334.66158130829))
    array.push(B0_e, core.vsop_term.new(0.00000101643, 5.42248619256, 5507.55323866740))

    array<core.vsop_term> B1_e = array.new<core.vsop_term>()
    array.push(B1_e, core.vsop_term.new(0.00000009030, 3.89729061890, 5507.55323866740))

    float B0sum_e = core.eval_vsop87(B0_e, t)
    float B1sum_e = core.eval_vsop87(B1_e, t)
    float lat_rad_e = B0sum_e + B1sum_e * t

    float dB0_e = core.eval_vsop87_derivative(B0_e, t)
    float dB1_e = core.eval_vsop87_derivative(B1_e, t)
    float dlat_dt_e = dB0_e + B1sum_e + t * dB1_e

    array<core.vsop_term> R0_e = array.new<core.vsop_term>()
    array.push(R0_e, core.vsop_term.new(1.00000101778, 0.0, 0.0))
    array.push(R0_e, core.vsop_term.new(0.01670861753, 3.09846350771, 6283.07584999140))
    array.push(R0_e, core.vsop_term.new(0.00013956023, 3.05524609620, 12566.15169998280))
    array.push(R0_e, core.vsop_term.new(0.00003083720, 5.19846674381, 77713.77146812050))
    array.push(R0_e, core.vsop_term.new(0.00001628461, 1.17387749012, 5753.38488489680))

    array<core.vsop_term> R1_e = array.new<core.vsop_term>()
    array.push(R1_e, core.vsop_term.new(0.00005641515, 4.29697744294, 6283.07584999140))
    array.push(R1_e, core.vsop_term.new(0.00001252975, 2.69402198377, 12566.15169998280))

    array<core.vsop_term> R2_e = array.new<core.vsop_term>()
    array.push(R2_e, core.vsop_term.new(0.00004392, 0.0, 0.0))

    float R0sum_e = core.eval_vsop87(R0_e, t)
    float R1sum_e = core.eval_vsop87(R1_e, t)
    float R2sum_e = core.eval_vsop87(R2_e, t)
    float r_e = R0sum_e + R1sum_e * t + R2sum_e * t * t

    float dR0_e = core.eval_vsop87_derivative(R0_e, t)
    float dR1_e = core.eval_vsop87_derivative(R1_e, t)
    float dR2_e = core.eval_vsop87_derivative(R2_e, t)
    float dr_dt_e = dR0_e + R1sum_e + t * dR1_e + 2.0 * t * R2sum_e + t * t * dR2_e

    float cl_e = math.cos(lon_rad_e)
    float sl_e = math.sin(lon_rad_e)
    float cb_e = math.cos(lat_rad_e)
    float sb_e = math.sin(lat_rad_e)

    float x_e = r_e * cb_e * cl_e
    float y_e = r_e * cb_e * sl_e

    float vx_e = dr_dt_e * cb_e * cl_e - r_e * dlat_dt_e * sb_e * cl_e - r_e * cb_e * sl_e * dlon_dt_e
    float vy_e = dr_dt_e * cb_e * sl_e - r_e * dlat_dt_e * sb_e * sl_e + r_e * cb_e * cl_e * dlon_dt_e


    float geo_x = x_j - x_e
    float geo_y = y_j - y_e
    float v_geo_x = vx_j - vx_e
    float v_geo_y = vy_j - vy_e

    float denom = geo_x * geo_x + geo_y * geo_y
    float dlambda_dt = (geo_x * v_geo_y - geo_y * v_geo_x) / denom

    dlambda_dt * RAD_TO_DEG / 365250.0



// ────────────────────────────── GEO ──────────────────────────────
// @function Computes Jupiter's geocentric ecliptic longitude (as seen from Earth).
// @param t (float) Julian millennia from J2000.0 (use core.get_julian_millennia(time)).
// @returns (float) Geocentric ecliptic longitude in degrees, normalized to range [0, 360).
export get_geo_lon(float t) => // accurate might be able to improve by adding more terms to earth
    [earth_x, earth_y, earth_z] = core.get_earth_helio_coords(t)

    jupiter_lon_rad = get_helio_lon(t) * PI / 180.0
    jupiter_b_rad   = get_helio_lat(t) * PI / 180.0
    jupiter_r       = get_helio_radius(t)

    jupiter_x = jupiter_r * math.cos(jupiter_b_rad) * math.cos(jupiter_lon_rad)
    jupiter_y = jupiter_r * math.cos(jupiter_b_rad) * math.sin(jupiter_lon_rad)

    geo_x = jupiter_x - earth_x
    geo_y = jupiter_y - earth_y

    core.mod360(core.custom_atan2(geo_y, geo_x) * RAD_TO_DEG)


// @function Computes Jupiter's geocentric ecliptic latitude (as seen from Earth).
// @param t (float) Julian millennia from J2000.0 (use core.get_julian_millennia(time)).
// @returns (float) Geocentric ecliptic latitude in degrees, range approximately [-2, +2].
export get_geo_ecl_lat(float t) =>
    lon_rad = get_helio_lon(t) * PI / 180.0
    lat_rad = get_helio_lat(t)  // already radians
    r = get_helio_radius(t)

    jupiter_x = r * math.cos(lat_rad) * math.cos(lon_rad)
    jupiter_y = r * math.cos(lat_rad) * math.sin(lon_rad)
    jupiter_z = r * math.sin(lat_rad)

    [earth_x, earth_y, earth_z] = core.get_earth_helio_coords(t)

    geo_x = jupiter_x - earth_x
    geo_y = jupiter_y - earth_y
    geo_z = jupiter_z - earth_z

    r_xy = math.sqrt(geo_x * geo_x + geo_y * geo_y)
    core.custom_atan2(geo_z, r_xy) * RAD_TO_DEG


// @function Computes Jupiter's geocentric equatorial declination (as seen from Earth).
// @param t (float) Julian millennia from J2000.0 (use core.get_julian_millennia(time)).
// @returns (float) Geocentric equatorial declination in degrees, range [-90, +90] where positive is north.
export get_geo_decl(float t) =>
    float ecl_lon_deg = get_geo_lon(t)
    float ecl_lat_deg = get_geo_ecl_lat(t)
    float ecl_lon_rad = ecl_lon_deg * PI / 180.0
    float ecl_lat_rad = ecl_lat_deg * PI / 180.0
    float epsilon_deg = core.get_obliquity(t)
    float epsilon_rad = epsilon_deg * PI / 180.0

    float sin_delta = math.sin(ecl_lat_rad) * math.cos(epsilon_rad) + math.cos(ecl_lat_rad) * math.sin(epsilon_rad) * math.sin(ecl_lon_rad)
    float delta_rad = math.asin(sin_delta)
    delta_rad * RAD_TO_DEG

// ═══════════════════════════════════════════════════════════════════════════
// SHOWCASE PLOT - Uncomment for TradingView publication
// Demonstrates Jupiter geocentric data with 100-bar future projection
// ═══════════════════════════════════════════════════════════════════════════

var int LOOK_AHEAD = 250

// Polyline references (persist across bars)
var polyline lon_line = na
var polyline decl_line = na
var polyline speed_line = na

t = core.get_julian_millennia(time)

// Historical data
lon = get_geo_lon(t)
decl = get_geo_decl(t)
speed = get_geo_speed(t)

plot(lon, "Jupiter Geocentric Longitude", color=color.purple)
plot(decl, "Jupiter Declination", color=color.red)
plot(speed * 100, "Jupiter Speed x100", color=color.white)

// Future projection using polylines
if barstate.islast
    // Delete old polylines
    polyline.delete(lon_line)
    polyline.delete(decl_line)
    polyline.delete(speed_line)

    // Build point arrays (start with current bar for smooth connection)
    array<chart.point> lon_pts = array.new<chart.point>()
    array<chart.point> decl_pts = array.new<chart.point>()
    array<chart.point> speed_pts = array.new<chart.point>()

    // First point = current bar (connects polyline to historical plot)
    array.push(lon_pts, chart.point.from_time(time, lon))
    array.push(decl_pts, chart.point.from_time(time, decl))
    array.push(speed_pts, chart.point.from_time(time, speed * 100))

    // Add future points
    for i = 1 to LOOK_AHEAD
        future_time = core.get_future_time(time, i)
        future_t = core.get_julian_millennia(future_time)

        f_lon = get_geo_lon(future_t)
        f_decl = get_geo_decl(future_t)
        f_speed = get_geo_speed(future_t)

        array.push(lon_pts, chart.point.from_time(future_time, f_lon))
        array.push(decl_pts, chart.point.from_time(future_time, f_decl))
        array.push(speed_pts, chart.point.from_time(future_time, f_speed * 100))

    // Create polylines
    lon_line := polyline.new(lon_pts, xloc=xloc.bar_time,
                line_color=color.new(color.purple, 50), line_style=line.style_dotted, force_overlay=false)
    decl_line := polyline.new(decl_pts, xloc=xloc.bar_time,
                 line_color=color.new(color.red, 50), line_style=line.style_dotted, force_overlay=false)
    speed_line := polyline.new(speed_pts, xloc=xloc.bar_time,
                  line_color=color.new(color.white, 50), line_style=line.style_dotted, force_overlay=false)

